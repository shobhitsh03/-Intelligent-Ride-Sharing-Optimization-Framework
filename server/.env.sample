MONGODB_URI=mongodb://localhost:27017/carpool_db
MONGODB_DB=carpool_db
JWT_SECRET=replace_with_a_secure_secret
CLIENT_URL=http://localhost:5173
PORT=5000

# Optional payment / AI / SMTP keys
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
STRIPE_SECRET_KEY=
HF_API_KEY=

# SMTP (optional) - if omitted, password reset will use Ethereal test inbox
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
FROM_EMAIL="Carpool <no-reply@carpool.test>"
